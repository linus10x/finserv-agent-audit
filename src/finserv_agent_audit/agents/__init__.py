"""Reference agent surfaces (Tranche 2 in progress).

v1.1 will ship:
    AuditConsumer (base; accepts 4 Protocol seams + VendorScoreGate via one injection contract)
    AuditAgent, MonitorAgent, OrchestratorAgent (reference implementations)
"""
