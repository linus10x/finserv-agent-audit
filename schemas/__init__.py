"""DEPRECATED in v1.1 — import from `finserv_agent_audit.schemas` instead.

This package's re-export shims are kept for backward compatibility through the v1.1.x line;
removed in v1.2.
"""

__version__ = "1.1.0.dev0"
